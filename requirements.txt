google-generativeai
# >=1.57 required: st.bottom, alert titles, :shimmer[], container autoscroll.
# Floor raised to 1.60.0 to guarantee its security hardening ships in
# deployments: host-message origin spoofing rejection (CWE-346), query-string
# size/field caps (CWE-770), and the server.maxWidgetStateSize payload cap.
streamlit>=1.60.0
python-dotenv
pypdf2
pydub>=0.25.0
# Optional: enables the AssemblyAI diarization + merge speaker path
# (only used when ASSEMBLYAI_API_KEY is set and the toggle is on).
assemblyai
